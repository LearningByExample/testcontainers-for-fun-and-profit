## PostgreSQL
spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/customers
        username: estefania
        password: estefaniapwd

    ## drop n create table again, good for testing, comment this in production
    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
            ddl-auto: create
        show-sql: true
